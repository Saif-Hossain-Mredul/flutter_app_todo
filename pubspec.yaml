name: flutter_app_todo
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  intl: ^0.16.0
  firebase_core: ^0.4.0
  firebase_auth: ^0.16.0
  firebase_storage: ^3.0.3
  cloud_firestore:
  path_provider: ^1.3.0
  flutter_slidable: ^0.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

